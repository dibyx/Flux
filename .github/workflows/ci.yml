name: CI Tests

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  test-python:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'
      - name: Test assembler
        run: python sw-toolchain/asm/assembler.py examples/vecadd.s
      - name: Test simulator
        run: python sw-toolchain/sim/simulator.py examples/vecadd.hex
